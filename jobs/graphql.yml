job: graphql-server
datacenters: [ srnd ]

tasks:
  graphql-server:
    image: "srnd/gql-server"

    resources:
      memory: 128

    ports:
      http:
        inner: 4000
        lb:
          domain: graph.srnd.org
          cert: srnd.org
