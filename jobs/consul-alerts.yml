job: consul-alerts
datacenters: [ srnd ]

tasks:
  discord0:
    image: "acaleph/consul-alerts"

    args:
      - start
      - --consul-addr=m.codeday.cloud:8500
      - --watch-events
      - --watch-checks